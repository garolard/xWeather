<views:MvxWindowsPage
    x:Class="XWeather.Uwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XWeather.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.WindowsUWP.Views"
    xmlns:viewModels="using:XWeather.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{StaticResource DesignViewModel}">

    <views:MvxWindowsPage.Resources>
        <viewModels:MainViewModel x:Key="DesignViewModel" />
    </views:MvxWindowsPage.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBox Text="{Binding CityName, Mode=TwoWay}"/>
            <Button Content="Get weather" Command="{Binding GetCurrentWeatherCommand}"/>
            <TextBlock Margin="0 12 0 0" Text="{Binding CurrentWeather.Main.Temp}"/>
            <TextBlock Margin="0 12 0 0" Text="{Binding CurrentWeather.Clouds.All}"/>
        </StackPanel>
    </Grid>
</views:MvxWindowsPage>
